import { encodeFunctionData, getAddress } from "viem";
import { getAction } from "viem/utils";
import { parseAccount } from "../../utils/index.js";
import { AccountOrClientNotFoundError } from "../../utils/signUserOperationHashWithECDSA.js";
import { sendUserOperation } from "../smartAccount/sendUserOperation.js";
import { parseModuleTypeId } from "./supportsModule.js";
export async function uninstallModules(client, parameters) {
    const { account: account_ = client.account, maxFeePerGas, maxPriorityFeePerGas, nonce, middleware, modules } = parameters;
    if (!account_) {
        throw new AccountOrClientNotFoundError({
            docsPath: "/docs/actions/wallet/sendTransaction"
        });
    }
    const account = parseAccount(account_);
    const uninstallModulesCallData = await account.encodeCallData(await Promise.all(modules.map(({ type, address, context }) => ({
        to: account.address,
        value: BigInt(0),
        data: encodeFunctionData({
            abi: [
                {
                    name: "uninstallModule",
                    type: "function",
                    stateMutability: "nonpayable",
                    inputs: [
                        {
                            type: "uint256",
                            name: "moduleTypeId"
                        },
                        {
                            type: "address",
                            name: "module"
                        },
                        {
                            type: "bytes",
                            name: "deInitData"
                        }
                    ],
                    outputs: []
                }
            ],
            functionName: "uninstallModule",
            args: [
                parseModuleTypeId(type),
                getAddress(address),
                context
            ]
        })
    }))));
    return getAction(client, (sendUserOperation), "sendUserOperation")({
        userOperation: {
            sender: account.address,
            maxFeePerGas: maxFeePerGas,
            maxPriorityFeePerGas: maxPriorityFeePerGas,
            callData: uninstallModulesCallData,
            nonce: nonce
        },
        account: account,
        middleware
    });
}
//# sourceMappingURL=uninstallModules.js.map