import { useMemo, isValidElement } from 'react';
import { fundWalletSvg } from '../../internal/svg/fundWallet.js';
import { swapSettingsSvg } from '../../internal/svg/swapSettings.js';
import { walletSvg } from '../../internal/svg/walletSvg.js';
const useIcon = ({
  icon
}) => {
  return useMemo(() => {
    if (icon === undefined) {
      return null;
    }
    switch (icon) {
      case 'fundWallet':
        return fundWalletSvg;
      case 'swapSettings':
        return swapSettingsSvg;
      case 'wallet':
        return walletSvg;
    }
    if ( /*#__PURE__*/isValidElement(icon)) {
      return icon;
    }
  }, [icon]);
};
export { useIcon };
//# sourceMappingURL=useIcon.js.map
