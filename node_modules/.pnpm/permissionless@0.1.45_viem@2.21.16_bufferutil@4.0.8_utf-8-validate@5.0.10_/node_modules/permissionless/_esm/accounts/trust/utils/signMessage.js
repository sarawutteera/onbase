import { signMessage as viem_signMessage } from "viem/actions";
export const signMessage = (client, { account, message }) => {
    return viem_signMessage(client, { account, message });
};
//# sourceMappingURL=signMessage.js.map