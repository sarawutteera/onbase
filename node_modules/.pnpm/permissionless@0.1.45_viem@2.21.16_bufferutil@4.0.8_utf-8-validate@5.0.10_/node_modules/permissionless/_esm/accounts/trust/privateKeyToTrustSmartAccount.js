import { privateKeyToAccount } from "viem/accounts";
import { signerToTrustSmartAccount } from "./signerToTrustSmartAccount.js";
/**
 * @description Creates an Trust Account from a private key.
 *
 * @returns A Private Key Trust Account.
 */
export async function privateKeyToTrustSmartAccount(client, { privateKey, ...rest }) {
    const privateKeyAccount = privateKeyToAccount(privateKey);
    return signerToTrustSmartAccount(client, {
        signer: privateKeyAccount,
        ...rest
    });
}
//# sourceMappingURL=privateKeyToTrustSmartAccount.js.map