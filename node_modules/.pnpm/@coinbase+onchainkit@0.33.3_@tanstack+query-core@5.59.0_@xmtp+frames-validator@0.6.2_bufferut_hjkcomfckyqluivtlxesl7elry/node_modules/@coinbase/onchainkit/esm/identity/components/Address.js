import { cn, text } from '../../styles/theme.js';
import { getSlicedAddress } from '../utils/getSlicedAddress.js';
import { useIdentityContext } from './IdentityProvider.js';
import { jsx } from 'react/jsx-runtime';
function Address({
  address = null,
  className,
  isSliced = true
}) {
  const _useIdentityContext = useIdentityContext(),
    contextAddress = _useIdentityContext.address;
  if (!contextAddress && !address) {
    throw new Error('Address: an Ethereum address must be provided to the Identity or Address component.');
  }
  const accountAddress = address ?? contextAddress;
  return /*#__PURE__*/jsx("span", {
    "data-testid": "ockAddress",
    className: cn(text.label2, className),
    children: isSliced ? getSlicedAddress(accountAddress) : accountAddress
  });
}
export { Address };
//# sourceMappingURL=Address.js.map
